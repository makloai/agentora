# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Run `pnpm changeset`
to record a version bump for the packages you changed; CI's release workflow opens a "Version
Packages" PR and publishes `@agentora/*` to npm when it merges.
